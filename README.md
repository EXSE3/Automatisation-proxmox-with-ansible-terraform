# Automatisation-proxmox-with-ansible-terraform
In this project , we learn about yhe hypervisor proxmox , create the dynamic inventory with ansible(or python), and provissiong virtual vms with terraform
